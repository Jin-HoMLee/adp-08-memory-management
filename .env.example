# Example environment variables for GCP configuration
# Copy this file to .env and update the values accordingly.
# Do not commit sensitive information to version control.

GCP_PROJECT_ID = your-gcp-project-id
GCP_LOCATION = your-gcp-location
GOOGLE_API_KEY = your-google-api-key

# Do not commit sensitive information to version control.
